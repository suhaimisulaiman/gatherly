# =============================================================================
# Gatherly — Vercel + Supabase
# =============================================================================
# Copy to .env.local and fill in values. Never commit .env.local.

# --- Supabase (required for auth & database) ---
# From: https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
# Publishable key (preferred) or legacy anon key
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_publishable_key
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# Optional: server-only key for admin/backend (never expose to client)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Admin: comma-separated emails allowed to access /admin and manage config
# ADMIN_EMAILS=admin@example.com,other@example.com

# Optional: direct Postgres URL for migrations / server-only DB tools (URL-encode password)
# SUPABASE_DB_URL=postgresql://postgres:YOUR_URL_ENCODED_PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres

# --- Vercel (optional; set in Vercel dashboard for deploys) ---
# Vercel auto-injects VERCEL_URL, VERCEL_ENV. No action needed for basic deploy.
# For preview/analytics: add env vars in Vercel Project → Settings → Environment Variables

# --- Future ---
# STRIPE_SECRET_KEY=
# RESEND_API_KEY=
# S3/R2 for storage when needed
